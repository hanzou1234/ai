services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - DATABASE_URL=sqlite+aiosqlite:///./agent_economy.db
      - PLATFORM_FEE_RATE=0.05
    env_file:
      - .env
