services:
  - type: web
    name: agent-marketplace
    env: docker
    region: singapore
    plan: free
    envVars:
      - key: PLATFORM_FEE_RATE
        value: "0.05"
      - key: LOG_LEVEL
        value: "INFO"
